using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameSettings : MonoBehaviour
{
    // Checks, if pitch is inversed
    public static bool IsPitchInverted;
    // Control type
    public static ControlType ControlType;
}
